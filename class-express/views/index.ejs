  <h1>hello <%= user %></h1>
  <% if (isAdmin) { %>
    <h3>hola eres admin</h3>
  <% } else { %>
    <h3>no eres admin</h3>
  <% } %>
  <ul>
    <% arr.forEach((city) => { %>
      <li><%= city %></li>
    <% }) %>
  </ul>
  <%- include('partials/footer') %>
